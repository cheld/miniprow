rules:
  - if:
      trigger: github_comment 
      when:
        contains: /needs-performance-tests
    then:
      action: debug
      with:
        comment: Performance tests are scheduled

  - if:
      trigger: github_comment
      when:
        contains: asdfs
    then:
      action: debug
      with:
        comment: Performance tests are scheduled
