
rules:
  - when:
      trigger: github_comment 
      if:
        contains: /cat
    then:
      action: github_comment
      with:
        comment: Grumpy cat



rules:
  - if:
      trigger: github_comment 
      when:
        contains: /cat
    then:
      action: github_comment
      with:
        comment: Grumpy cat

